// Picked up automatically by web-ext (see mozilla/web-ext config discovery).
// experiment_apis is only valid when the add-on is treated as privileged for lint.
export default {
  lint: {
    privileged: true,
  },
};
